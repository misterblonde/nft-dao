export function voteFailsBecauseAlreadyVoted(): void {
  it("Contract releases funds to the proposal creator", async function () {
    // const test = await loadFixture(setupInitialState);

    // mint 3 NFTS
    const myFirstMint = await this.token.connect(this.addr2);
  });
}
